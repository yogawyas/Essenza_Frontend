<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@color/essenza_background</item>
        <item name="android:navigationBarColor">@color/essenza_background</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowSplashScreenBackground">@color/essenza_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/essenza_splash_icon</item>
    </style>

</resources>
